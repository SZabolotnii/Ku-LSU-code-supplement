# Python dependencies for the Paper 4 verification gate + figures.
# Verified environment (the numbers quoted in the paper reproduce here, seed 2026):
#   Python 3.13 / 3.14, numpy 2.4.4, matplotlib 3.10.9
# The gate uses only numpy; figures additionally use matplotlib. scipy is NOT required.
# Monte-Carlo quantities are checked against tolerances, so trailing digits may differ
# across BLAS builds while every PASS criterion remains robust.
numpy>=2.0,<3
matplotlib>=3.8,<4
